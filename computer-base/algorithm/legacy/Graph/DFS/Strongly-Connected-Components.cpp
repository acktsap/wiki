
====== Strongly-Connected-Components ======



 o Strongly Connected Components

    In directed graph,
    if ∀ u,v ∈ some set of V, ∃ some path u -> v, v -> u
    then the set of V is called Strongly Connected Components.


 o DAG & SSC

    We can make DAG if we consider SSC as a vertex

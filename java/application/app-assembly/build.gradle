apply plugin: 'distribution'

dependencies {
  implementation project(':app-core')
  implementation project(':app-tools')
}

distributions {
  main {
    baseName = 'java-application'
    contents {
      from {'src/assembly'}
      into('lib') {
        from (project.configurations.runtimeClasspath)
      }
    }
  }
}

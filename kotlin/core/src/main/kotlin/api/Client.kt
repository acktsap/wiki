package api

import api.total1 as total

// change like this
// import api.total2 as total

fun main() {
    Student("lim", 100, 100).total();
    // actual code in java
    // FuncKt.total1(new Student("name", 0, 0));
}

